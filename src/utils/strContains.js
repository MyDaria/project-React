const strContains = (fstString, sndString) => {
    if (fstString.toLowerCase().includes(sndString.toLowerCase()))

        return true;
        
        else return false;
};

export default strContains;